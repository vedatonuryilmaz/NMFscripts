# requirements.txt

# Core data handling and numerics
numpy>=1.20.0
pandas>=1.3.0
scipy>=1.7.0

# For NMF and metrics
scikit-learn>=1.0.0

# For Polars (data loading and bool_map generation)
polars>=0.18.0 # Or a more recent version

# For plotting
matplotlib>=3.4.0

# For progress bars
tqdm>=4.60.0

# For parallelization
joblib>=1.0.0

# No specific libraries are needed just for reading JSON (it's in Python's standard library)